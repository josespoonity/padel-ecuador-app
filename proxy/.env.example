# Padel Ecuador proxy config
PORT=8787
SPOONITY_BASE=https://api.spoonity.com
VENDOR=2112777
LANGUAGE=3
CONSENT_FULL=3

# Comma-separated list of origins allowed to call this proxy.
# For local dev with the HTML opened via a static server, add its origin.
# Use * only for quick local testing.
ALLOWED_ORIGIN=*
