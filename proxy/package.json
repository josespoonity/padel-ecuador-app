{
  "name": "padel-ecuador-proxy",
  "version": "1.0.0",
  "description": "Padel Ecuador web app -> Spoonity Consumer API proxy. Runs standalone locally (server.js) or as a Firebase Cloud Function (index.js) — same routes either way, see app.js.",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-functions": "^6.0.1"
  }
}
